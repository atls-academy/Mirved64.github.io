import styled                 from '@emotion/styled'
import { Box }                from '@atls-ui-parts/layout'

import { baseBackdropStyles } from './backdrop.styles'

export const Backdrop = styled(Box)(baseBackdropStyles)
